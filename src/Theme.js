import {createTheme} from '@mui/material/styles';

export const theme = createTheme({
  primary: '#0673F2',
  secondary: '#3F3D3D',
  palette: {
    primary: {
      main: '#0673F2',
    },
    secondary: {
      main: '#3F3D3D',
    },
  },
});
